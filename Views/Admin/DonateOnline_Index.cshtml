@model List<GiveOnline.Models.DonateOnline>
@{
    ViewBag.Title = "Event_Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<table class="table">
    <thead class="thead-inverse">
        <tr>
            <td>Id</td>
            <td>Title</td>
            <td>Content</td>
            <td>Image</td>
            <td>Price</td>
            <td>Active Date</td>
            <td>Edit</td>
            <td>Delete</td>
        </tr>
    </thead>
    @foreach (var donateItem in Model)
    {
        <tr>
            <td>@donateItem.Id</td>
            <td>@donateItem.Title</td>
            <td>@donateItem.Content</td>
            <td><img class="img-responsive tableImage" src="@donateItem.ImageUrl" /></td>
            <td>@donateItem.Price</td>
            <td>@donateItem.IsActive</td>

            <td>
                <a href="/Admin/DonateOnline_Edit/@donateItem.Id">
                    <i class="glyphicon glyphicon-pencil" aria-hidden="true"></i>
                </a>
            </td>
            <td>
                <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#@donateItem.Id">
                    Delete
                </button>

                <div class="modal fade" id="@donateItem.Id" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-body">
                                <p>Are you sure you want to delete this?</p>
                                <p>there are no take backs!</p>
                            </div>
                            <div class="modal-footer">
                                <form action="/Admin/DonateOnline_Delete/@donateItem.Id" method="post">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                                <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </td>
        </tr>

    }


</table>

