@model GiveOnline.Models.VariantModel
@{
    ViewBag.Title = "Variants_Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<table class="table">
    <thead class="thead-inverse">
        <tr>
            <td><h3>Colors</h3></td>
            <td>Delete</td>
        </tr>
    </thead>
    @foreach (var colorItem in Model.Colores)
    {
        <tr>
            <td>@colorItem.Title</td>
            <td>
                <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#@colorItem.Id">
                    Delete
                </button>

                <div class="modal fade" id="@colorItem.Id" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-body">
                                <p>Are you sure you want to delete this?</p>
                                <p>there are no take backs!</p>
                            </div>
                            <div class="modal-footer">
                                <form action="/Admin/Color_Delete/@colorItem.Id" method="post">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                                <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </td>
        </tr>
    }

</table>


<table class="table">
    <thead class="thead-inverse">
        <tr>
            <td><h3>Size</h3></td>
            <td>Delete</td>
        </tr>
    </thead>
    @foreach (var sizeitem in Model.Sizes)
    {
        <tr>
            <td>@sizeitem.Title</td>
            <td>
                <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#@sizeitem.Id">
                    Delete
                </button>

                <div class="modal fade" id="@sizeitem.Id" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-body">
                                <p>Are you sure you want to delete this?</p>
                                <p>there are no take backs!</p>
                            </div>
                            <div class="modal-footer">
                                <form action="/Admin/Size_Delete/@sizeitem.Id" method="post">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                                <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </td>
        </tr>
    }

</table>

