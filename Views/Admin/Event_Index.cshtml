@model List<GiveOnline.Models.Event>
@{
    ViewBag.Title = "Event_Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<table class="table">
    <thead class="thead-inverse">
        <tr>
            <td>Id</td>
            <td>Title</td>
            <td>Content</td>
            <td>Image</td>
            <td>Date</td>
            <td>Edit</td>
            <td>Delete</td>
        </tr>
    </thead>
    @foreach (var eventItem in Model)
    {
        <tr>
            <td>@eventItem.Id</td>
            <td>@eventItem.Title</td>
            <td>@eventItem.Content</td>
            <td><img class="img-responsive tableImage" src="@eventItem.ImageUrl" /></td>
            <td>@eventItem.Date</td>
            <td>
                <a href="/Admin/Event_Edit/@eventItem.Id">
                    <i class="glyphicon glyphicon-pencil" aria-hidden="true"></i>
                </a>
            </td>
            <td>
                <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#@eventItem.Id">
                    Delete
                </button>

                <div class="modal fade" id="@eventItem.Id" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-body">
                                <p>Are you sure you want to delete this?</p>
                                <p>there are no take backs!</p>
                            </div>
                            <div class="modal-footer">
                                <form action="/Admin/Event_Delete/@eventItem.Id" method="post">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                                <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </td>
        </tr>

    }


</table>

